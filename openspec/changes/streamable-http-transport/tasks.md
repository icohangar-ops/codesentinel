# Tasks

- [x] Extract shared `createMcpServer()` from stdio entry
- [x] Add stateless Streamable HTTP entry + fail-closed Bearer auth
- [x] Health endpoints; never echo secrets
- [x] Update `server.json`, package scripts, `.env.sample`, README, Dockerfile
- [x] Deploy notes (Fly/Railway/Docker; Vercel not recommended)
- [x] Tests + smoke script for health / 401 / initialize
- [x] `npm test` passes
