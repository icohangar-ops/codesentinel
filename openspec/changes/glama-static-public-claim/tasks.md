# Tasks

- [x] Add `public/.well-known/glama.json` and `public/glama.json` with the current claim
- [x] Set `outputDirectory` to `public` and remove the well-known rewrite
- [x] Keep `/mcp` `/health` `/healthz` → `/api` and Fluid `api/index.mjs`
- [x] Update Glama claim tests and MCP HTTP docs
- [x] `npm test` for claim + HTTP/auth suites
