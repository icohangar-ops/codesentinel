{
  "name": "codesentinel-mcp-server",
  "version": "1.0.0",
  "description": "CodeSentinel MCP Server — codebase analysis plus remote MCP protocol health (handshake ≠ HTTP 200)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "http": "node http.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^4.21.2"
  }
}